name: Render KeepAlive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -fsS https://kool-klown-klanx-discord-bot.onrender.com >/dev/null
